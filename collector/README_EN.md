# Collector

The collector runs on the acquisition computer. It records device streams and uploads session batches to the processor. Server-side decoding, AI annotation, review and LeRobot export belong under `processor/`.
