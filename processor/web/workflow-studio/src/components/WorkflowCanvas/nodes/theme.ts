/** Semantic module colors used by the canvas fallback registry.
 * The backend catalog is the runtime source of truth; these values keep the
 * first paint and offline mode consistent with it. */
export const MODULE_COLORS = {
  input: '#3b82f6',
  hand3d: '#06b6d4',
  annotation: '#f97316',
  review: '#f59e0b',
  export: '#22c55e',
} as const;
